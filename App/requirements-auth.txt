# Runtime dependencies unique to the auth + docs-rendering layer.
# PyJWT, bcrypt, and cryptography are provided by requirements.txt (both files
# are installed into the same /install prefix in the Dockerfile builder).
markdown==3.7
# nh3: HTML sanitizer for rendered Markdown (docs viewer).
nh3>=0.2.18
